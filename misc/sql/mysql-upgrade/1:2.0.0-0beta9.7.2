ALTER TABLE `notificationSchedules` ADD `nlimit` MEDIUMINT UNSIGNED NOT NULL DEFAULT '0';

CREATE TABLE IF NOT EXISTS `notificationsSent` (
  `rule_id` smallint(5) unsigned NOT NULL,
  `time` int(11) NOT NULL
) ENGINE=InnoDB DEFAULT CHARSET=latin1;

INSERT INTO `bluecherry`.`GlobalSettings` (
`parameter` ,
`value`
) VALUES 
('G_SMTP_USERNAME', NULL), 
('G_SMTP_PASSWORD', NULL), 
('G_SMTP_HOST', NULL), 
('G_SMTP_PORT', '25'), 
('G_SMTP_SSL', 'none'), 
('G_SMTP_FAIL', NULL), 
('G_SMTP_SERVICE', 'default');

