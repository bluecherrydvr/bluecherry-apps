#!/bin/sh

set -e

sql="/usr/share/doc/bluecherrydvr/sqlite-base.sql"
db="/var/lib/bluecherry/bluecherry.sqlite"

case "$!" in
	configure)
		test -f "$sql" && (test -f "$db" || sqlite3 "$db" < "$sql")
		;;
esac

#DEBHELPER#
