#!/bin/sh

set -e

dbdir="/var/lib/bluecherry"
db="$dbdir/bluecherry.sqlite"

case "$1" in
	purge)
		rm -f "$db"
		if test -d "$dbdir"; then
			 rmdir --ignore-fail-on-non-empty "$dbdir"
		fi
		userdel bluecherry || true
		groupdel bluecherry || true
		;;
esac

#DEBHELPER#
